% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krls.R
\name{krls}
\alias{krls}
\alias{krls.formula}
\alias{krls.default}
\alias{predict.krls_rr}
\title{Kernel Regularized Least Squares}
\usage{
krls(X, ...)

\method{krls}{formula}(X, data = NULL, subset = NULL, ..., y = NULL)

\method{krls}{default}(
  X,
  y,
  sigma = NULL,
  lambda = NULL,
  derivative = TRUE,
  binary = TRUE,
  vcov = TRUE,
  weights = NULL,
  subset = NULL,
  L = NULL,
  U = NULL,
  tol = NULL,
  eigtrunc = NULL,
  lambda.method = c("loo", "gcv", "cv"),
  lambda.grid = NULL,
  nfold = 0L,
  ncross = NULL,
  stratify = TRUE,
  seed.cv = NULL,
  cv.1se = FALSE,
  autotune = FALSE,
  autotune.grid = NULL,
  autotune.nthreads = NULL,
  autotune.speed = c("balanced", "quality", "fast"),
  autotune.warmstart = TRUE,
  varmod = c("none", "const"),
  n.boot = 0L,
  na.action = c("impute", "omit"),
  approx = c("exact", "nystrom"),
  nystrom_m = NULL,
  landmarks = NULL,
  landmark_method = c("random", "kmeans"),
  landmark_seed = NULL,
  nystrom_eps = 1e-09,
  ard = c("none", "cheap"),
  ard.alpha = 1,
  ard.cap = 100,
  ard.imp = c("avgderiv", "vsq"),
  trace = NULL,
  nthreads = 0L,
  print.level = NULL,
  ...
)

\method{predict}{krls_rr}(
  object,
  newdata = NULL,
  se.fit = FALSE,
  interval = c("none", "pint"),
  level = 0.95,
  type = c("response", "link", "prob"),
  ...
)
}
\arguments{
\item{X}{A numeric matrix or data frame of predictors (\verb{n x p}).
Constant columns are rejected. Factor / character columns in a
data frame are expanded via \code{model.matrix(~ ., x)} (treatment
contrasts, intercept dropped). Missing values are handled via
\code{na.action} (see below).}

\item{...}{Currently unused (caught for forward compatibility).}

\item{data}{Used only by the formula method. A data frame containing
the variables referenced by the formula.}

\item{subset}{Optional integer or logical vector restricting which
rows of \code{data} / \code{X} are used for the fit. Mirrors the
formula-method behaviour. When \code{X} is supplied directly
(matrix or data frame), \code{subset} slices \code{X}, \code{y}, and \code{weights}
immediately after input validation; \code{NULL} (default) is a no-op
and back-compatible.}

\item{y}{A numeric response vector or single-column matrix.  Constant
\code{y} is rejected.}

\item{sigma}{Gaussian-kernel bandwidth.  Default \code{NULL}, which sets
sigma via the geomean_p formula: \code{sqrt(median(d2) * p)} where \code{d2}
are pairwise squared Euclidean distances on the standardised
predictors and \code{p = ncol(X)}. May be supplied as a strictly positive
scalar OR (since v0.0.0.9045) as a length-\code{ncol(X)} vector of
per-feature lengthscales (manual ARD): the Gaussian kernel becomes
\code{K_ij = exp(-sum_k (x_ik - x_jk)^2 / sigma_k)}. Vector form is
incompatible with \code{autotune = TRUE} and with \code{approx = "nystrom"}
in this version; both error at fit time. For per-feature \code{sigma},
lengthscales apply on the column-standardised predictor matrix
used internally (a value of \code{1} corresponds to one unit of
\code{sd(X[, k])}).}

\item{lambda}{Optional ridge penalty.  If \code{NULL} (default), selected
by golden-section search on the LOO error.}

\item{derivative}{Logical.  If \code{TRUE} (default), compute pointwise
marginal effects and their average per variable.  Requires \code{vcov}.}

\item{binary}{Logical.  If \code{TRUE} (default), columns of \code{X} with
exactly two unique values are treated as binary and their marginal
effects are replaced by predicted-Y first differences (matches
\code{KRLS::fdskrls}).}

\item{vcov}{Logical.  If \code{TRUE} (default), compute the coefficient
covariance and the variance of average marginal effects.}

\item{weights}{Optional vector of observation weights (length \code{n},
strictly positive). Internally normalised to mean 1. Implements
weighted KRLS via a \verb{D K D} transform where \code{D = diag(sqrt(w))}.
\code{weights = rep(1, n)} is byte-identical to the unweighted path.}

\item{L, U}{Optional lower / upper bracket for the lambda search.  If
\code{NULL}, defaults follow \code{KRLS::krls()}.}

\item{tol}{Tolerance for the lambda golden section.  Default \code{1e-6}
(fixed, independent of \code{n}; was \code{1e-3 * n} in earlier versions).
A fixed small tolerance gives 6-digit lambda precision regardless
of sample size.}

\item{eigtrunc}{Optional eigenvalue truncation cutoff in \verb{(0, 1]}.
When set, eigenvalues below \code{eigtrunc * max(d)} are dropped from
the solve.  \code{NULL} (default) keeps all eigenvalues.}

\item{lambda.method}{Lambda-selection rule. \code{"loo"} (default) uses
the closed-form leave-one-out golden-section search; \code{"cv"}
uses K-fold CV over a grid (\code{nfold > 0} required). \code{"gcv"} uses
the closed-form generalised cross-validation criterion (Craven &
Wahba 1979) using the same eigendecomposition; recommended when
\code{n} is large or LOO behaves unstably.}

\item{lambda.grid}{Optional numeric vector of lambda candidates for
\code{lambda.method = "cv"}. \code{NULL} (default) auto-generates a
log-spaced grid in \verb{[L, U]}.}

\item{nfold}{Number of CV folds for \code{lambda.method = "cv"} or for
\code{autotune}. \code{0} (default) disables CV.}

\item{ncross}{Number of CV repetitions (each builds a fresh fold
partition). Default \code{NULL}, which resolves to \code{2} for the autotune
sigma search (repeated-CV stabilisation) and \code{1} for
\code{lambda.method = 'cv'}. Explicit integer values are honoured
in both paths.}

\item{stratify}{If \code{TRUE} (default), CV folds are quantile-
stratified on \code{y}.}

\item{seed.cv}{Optional integer seed for the CV fold partition.}

\item{cv.1se}{If \code{TRUE}, applies the one-standard-error rule when
picking lambda under CV (smallest model within 1 SE of the
minimum mean CV-MSE). Default \code{FALSE}.}

\item{autotune}{If \code{TRUE}, runs an inner repeated-CV grid search
over \code{sigma} (default: 9-point multiplicative grid centred on the
median-heuristic sigma anchor, from \code{anchor * 0.125} to
\code{anchor * 32}) and refits the winner on the full data using the
one-standard-error rule (selects the largest sigma within 1 SE of
the minimum CV-MSE). Default \code{FALSE}.}

\item{autotune.grid}{Optional numeric vector of \code{sigma} candidates
for autotune. \code{NULL} (default) uses the 9-point anchor-centred
grid \code{sigma_anchor * c(0.125, 0.25, 0.5, 1, 2, 4, 8, 16, 32)}
where \code{sigma_anchor} is the geomean_p anchor
(\code{sqrt(median(d2) * p)} on the standardised predictors).}

\item{autotune.nthreads}{Integer. Number of worker threads used to
evaluate the autotune \code{sigma} grid in parallel. \code{NULL} (default)
reads from \code{getOption("roadrunner.krls.autotune.nthreads")}, and
falls back to \code{1L} if unset. Capped at \code{length(autotune.grid)}.
Only used when \code{autotune = TRUE}.}

\item{autotune.speed}{Speed/quality knob for autotune dispatch. Only
used when \code{autotune = TRUE}. One of:
\itemize{
\item \code{"fast"}: scalar sigma grid only (v0.0.0.9046 behaviour exactly).
\item \code{"balanced"} (default): dispatches through cheap-tier ARD when
\code{ncol(X) >= 20} or \code{ncol(X) >= nrow(X) / 10} (heuristic for
high-p / sparse-signal regimes); otherwise scalar sigma only.
\item \code{"quality"}: always dispatches through ARD and sweeps a 6-cell
grid over \verb{ard.alpha in \{0.5, 1, 2\}} x
\verb{ard.imp in \{"avgderiv", "vsq"\}}; picks the winner by inner CV.
On low-p dense data \code{"balanced"} is byte-equivalent to \code{"fast"}.
}}

\item{autotune.warmstart}{If \code{TRUE} (default), autotune first pre-fits
on a 15\% subsample (capped at 200 rows) and runs one ARD probe; if
the probe does not improve held-out MSE over the isotropic baseline
by more than 2\%, the ARD dispatch branch is dropped and autotune
collapses to scalar sigma only. Skipped when \code{n < 200} or
\code{autotune.speed = "fast"}.}

\item{varmod}{Residual variance model used to construct prediction
intervals via \code{predict(..., interval = "pint")}. \code{"none"}
(default) disables PIs; \code{"const"} uses a homoscedastic
\code{sigma_hat} estimated from the training residuals.}

\item{n.boot}{Number of bootstrap replicates for bagging. \code{0}
(default) disables bagging. When \code{n.boot > 0}, prediction
averages across \code{n.boot} replicate fits.}

\item{na.action}{How to handle missing values in \code{X}. \code{"impute"}
(default) replaces NAs with the column median (stored on the fit
and reapplied at \code{predict()} time). \code{"omit"} drops rows with any
NA. Missing \code{y} is always an error.}

\item{approx}{Either \code{"exact"} (default) or \code{"nystrom"}. The exact
path runs the original O(n^3) eigendecomposition on the full kernel.
\code{"nystrom"} replaces it with a low-rank Nystrom approximation
anchored at \code{nystrom_m} landmarks, giving O(m^3) + O(n m^2) cost.
At default \code{nystrom_m = ceiling(sqrt(n) * 3)} test RMSE is typically
within +10\% of exact on smooth DGPs.}

\item{nystrom_m}{Integer. Number of landmarks when \code{approx = "nystrom"}.
Default \code{NULL} resolves to \code{ceiling(sqrt(n) * 3)} (e.g. n=2000 -> m=135,
n=5000 -> m=213). Tuned empirically to keep test-set RMSE within +10\%
of exact on smooth DGPs.
Ignored when \code{landmarks} is supplied explicitly.}

\item{landmarks}{NULL (default; auto-draw via \code{landmark_method}), an
integer vector of row indices into X, or an m x d numeric matrix of
landmark coordinates in the original X scale (auto-standardized
internally using the training centers/scales).}

\item{landmark_method}{Either \code{"random"} (default; uniform subsample of
training rows) or \code{"kmeans"} (Hartigan-Wong centers on standardized X;
not bit-stable across R versions). Ignored when \code{landmarks} is supplied.}

\item{landmark_seed}{Optional integer seed for the local landmark draw.
When supplied, uses \code{.with_seed()} to avoid disturbing the caller's
global RNG state. Required for byte-identical fits across consecutive
\code{krls(approx="nystrom")} calls.}

\item{nystrom_eps}{Numeric. Relative ridge floor applied to the
landmark-kernel eigenvalues: \code{D_reg = max(D, nystrom_eps * max(D))}.
Defaults to \code{1e-9}. Stabilizes the m x m eigendecomposition when
landmarks are near-collinear; the fit object's
\code{nystrom_diagnostics$floored_count} reports how many eigenvalues hit
this floor (useful for tuning m).}

\item{ard}{Automatic ARD (per-feature lengthscale) selector. \code{"none"}
(default) disables ARD selection; supply a scalar or length-\code{ncol(X)}
vector \code{sigma} manually. \code{"cheap"} enables a two-pass orchestrator:
pass 1 fits an isotropic anchor \code{krls()} at \code{sigma_anchor}, derives
per-feature importance from the average marginal effects (or row-mean-
square gradient, see \code{ard.imp}), and pass 2 refits with
\code{s_k = sigma_iso * (median(imp) / imp_k)^ard.alpha} clipped to
\verb{[sigma_iso / ard.cap, sigma_iso * ard.cap]}. Incompatible with
\code{approx = "nystrom"} and with a user-supplied vector \code{sigma}; both
error at fit time. When \code{autotune = TRUE} and the user supplies
\code{ard = "cheap"} explicitly, autotune routes through ARD regardless
of \code{autotune.speed}.}

\item{ard.alpha}{Mapping exponent on the importance ratio. \code{0} reduces
to isotropic; recommended range \verb{[0.5, 2.0]}. Default \code{1.0}.}

\item{ard.cap}{Symmetric multiplicative ceiling on per-feature
bandwidth: \code{s_k} is clipped to \verb{[sigma_iso / ard.cap, sigma_iso * ard.cap]}. Prevents eigendecomposition collapse on near-zero
importance features. Default \code{100}.}

\item{ard.imp}{Importance source for the cheap-tier mapping. \code{"avgderiv"}
(default) uses \verb{|avgderivatives[k]|}; \code{"vsq"} uses
\code{mean(derivatives[, k]^2)}, a slightly more robust signal.}

\item{trace}{Integer. \code{0} is silent, \verb{> 0} enables progress
diagnostics (currently: prints chosen lambda when \verb{> 1},
golden-section progress when \verb{> 2}). Replaces \code{print.level}.}

\item{nthreads}{Integer. Number of threads to use for the C++ kernel
build / decomposition. \code{0} (default) means use
\code{RcppParallel::defaultNumThreads()}.}

\item{print.level}{Deprecated alias for \code{trace}. Prefer \code{trace}.}

\item{object}{A fitted \code{"krls"} object.}

\item{newdata}{A numeric matrix or data frame with the same columns
as the training \code{X} (or matching the training formula).  \code{NULL}
returns \code{object$fitted}.}

\item{se.fit}{Logical.  If \code{TRUE}, return pointwise standard errors
of the predictions.  Requires the fit was created with
\code{vcov = TRUE}.}

\item{interval}{Prediction-interval mode. \code{"none"} (default)
returns point predictions only; \code{"pint"} returns lower/upper
bounds at confidence level \code{level}, requires the fit was
created with \code{varmod = "const"} (or other non-\code{"none"}
\code{varmod}).}

\item{level}{Confidence level for \code{interval = "pint"}. Default
\code{0.95}.}

\item{type}{Prediction type. \code{"response"} (default) returns
predictions on the original \code{y} scale. \code{"link"} is currently a
synonym for \code{"response"} (KRLS has no GLM link; reserved for
future logistic-loss extension). \code{"prob"} is valid only for
binary fits (\code{y} has exactly two unique values) and returns
\code{plogis(yhat)}. This is a calibration shortcut: the underlying
fit is still least-squares loss, not logistic loss; values are
clamped to \verb{[0, 1]} but are not true posterior probabilities.}
}
\value{
An object of S3 class \code{c("krls_rr", "krls")} with components
mirroring \code{KRLS::krls()}: \code{K}, \code{coeffs}, \code{Looe}, \code{fitted}, \code{X},
\code{y}, \code{sigma}, \code{lambda}, \code{R2}, \code{derivatives}, \code{avgderivatives},
\code{var.avgderivatives}, \code{vcov.c}, \code{vcov.fitted}, \code{binaryindicator}.
Formula-method fits additionally carry \code{call}, \code{terms}, \code{xlevels},
\code{factor_info}, \code{na.action}, and \code{na.medians} for downstream
\code{predict()}, \code{update()}, and \code{model.matrix()} support.

\code{Looe} follows the \code{KRLS::krls()} scale convention: it is the sum
of squared leave-one-out residuals on the \emph{standardised} \code{y} scale
multiplied by \code{sd(y)}, so its units are \verb{[y^2 / sd_y] = [y]}.  This
is preserved for downstream compatibility with code that consumed
\code{KRLS::krls()} output; it is \strong{not} the LOO MSE in raw-\code{y}
squared units.
}
\description{
Fits a Kernel Regularized Least Squares model with Gaussian kernel,
selecting the ridge penalty by leave-one-out cross-validation via the
closed-form identity of Hainmueller and Hazlett (2014).  Marginal
effects and their variances are computed by default and returned on
the original \verb{(X, y)} scale.
}
\details{
Three call styles are supported (mirrors \code{ares()}):
\itemize{
\item \code{krls(X, y, ...)}  -- matrix / numeric interface (back-compatible).
\item \code{krls(y ~ x1 + x2 + ..., data = df, ...)} -- formula interface
with factor expansion and derived terms (\code{I(x^2)}, \code{poly(x, 2)},
etc).
\item \code{krls(df, y, ...)} where \code{df} is a data frame with numeric +
factor / character columns -- expands categorical columns via
\code{model.matrix(~ ., df)} (treatment contrasts, intercept dropped).
}

The numerical pipeline mirrors \code{KRLS::krls()} exactly:
\enumerate{
\item \code{X} and \code{y} are standardised (column-centred, unit sd).
\item The Gaussian kernel \verb{K_ij = exp(-||x_i - x_j||^2 / sigma)} is built
in parallel C++.
\item \code{K} is eigendecomposed.  All subsequent solves use the
eigen-basis closed forms, never inverting \verb{K + lambda I} directly.
\item \code{lambda} is selected by golden-section search on the closed-form
LOO error sum, with the same \verb{(L, U, tol)} bracket as \code{KRLS::krls}.
\item Marginal effects use the closed-form identity
\verb{dy/dx_k = -(2/sigma) * (X_k * (K c) - K diag(c) X)_k}, computed
without forming the \verb{n x n} distance matrix.
\item Output is unstandardised back to the original \verb{(X, y)} scale.
}

At a fixed \verb{(sigma, lambda)}, fits agree with \code{KRLS::krls()} to
floating-point precision (typically \verb{< 1e-12} on coefficients,
fitted values, and marginal effects for \code{n <= 1000}).  Wall-clock
time is roughly \verb{6-10x} faster than \code{KRLS::krls()} at \code{n >= 500}
when marginal effects and variance estimates are requested.

Memory scales as \code{O(n^2)}: the kernel and its squared eigenvector
matrix are both stored.  Expect about \code{0.4 * n^2 / 1e6} MB of
peak working memory (e.g. ~400MB at \code{n = 1000}, ~10GB at \code{n = 5000}).

\strong{Scale-aware sigma default (geomean_p anchor, v0.0.0.9041)}

When \code{sigma = NULL}, roadrunner sets the Gaussian-kernel bandwidth using the
\emph{geomean_p} formula: let \verb{d2_ij = ||Xs_i - Xs_j||^2} be the pairwise squared
Euclidean distances on the standardised predictor matrix \code{Xs}; then
\code{sigma = sqrt(median({d2_ij : i < j}) * ncol(Xs))}.  This is the geometric
mean of the raw median heuristic and \code{p = ncol(X)}, and empirically lands in
the sweet spot between the two extremes.  At \verb{n=500, p=10} on standardised
N(0,1) data it yields \code{sigma ~ 13.5}, versus \code{~ 18-19} for the raw median
and \code{10} for \code{KRLS::krls()}'s fixed default.

\strong{Why geomean_p}: a 15-DGP head-to-head vs \code{KRLS::krls()} (REQ-20260518-003,
iter-2) with the geomean_p anchor shows roadrunner \strong{wins 12/15 DGPs, loses
0/15, ties 3/15}.  The raw median anchor (v0.0.0.9040) won only 4/15 DGPs
because it over-smoothed locally nonlinear signals (exp-decay, interaction,
tanh-interaction, poly2) by selecting sigma ~2x too wide.  The geomean_p
anchor preserves scale-awareness (adapts to the actual data distribution,
unlike the hard-coded \code{sigma = p}) while avoiding over-smoothing.

For \code{n > 500} the pairwise distance matrix is \code{O(n^2)}; to keep the default
cheap, a 500-row subsample is drawn with a fixed seed (\code{set.seed(2718)}) so
the result is deterministic within a session.

\strong{Autotune-equals-default equivalence at well-fit settings}

When autotune is enabled at settings where the default sigma is already near-
optimal (e.g. additive and interaction DGPs at \verb{n=500, p=10}), the autotune
grid is centred on \code{sigma_anchor} and the CV argmin coincides with the grid
centre.  The 1-SE rule then selects \code{sigma_anchor}, producing a fit identical
to the non-autotuned default.  This is the expected behaviour — it confirms
that the default sigma is well-chosen for these DGPs — not a failure of
autotune.  Autotune yields improvements when the optimal sigma departs from
the anchor (e.g. sparse / linear DGPs where a much wider kernel is better).

Since v0.0.0.9042 the autotune inner loop is parallelised over sigma
candidates using \code{RcppParallel} (TBB). The pairwise squared-distance
matrix is computed once per CV fold and reused across every sigma in
the grid (\code{exp(-D / sigma)} is then a cheap elementwise op).
Determinism is preserved: each worker writes to a unique slot in the
output vectors, so the result is byte-identical regardless of
\code{autotune.nthreads}. Pass \code{autotune.nthreads = 1} to force strictly
sequential execution.
}
\note{
At a fixed \verb{(sigma, lambda)} fits remain byte-identical to all earlier
versions of roadrunner KRLS.  The four defaults changed in v0.0.0.9040
(\code{sigma}, \code{tol}, autotune nfold/ncross, autotune grid) only affect results
when those arguments are left at \code{NULL} / at their default.  Restore any
old default explicitly: \code{sigma = ncol(X)}, \code{tol = 1e-3 * nrow(X)},
\code{nfold = 5}, \code{ncross = 1}, \code{autotune.grid = ncol(X) * c(0.25, 0.5, 1, 2, 4, 8)}.

The sigma anchor formula was refined in v0.0.0.9041 from the raw median
(\code{median(d2)}) to the geomean_p formula (\code{sqrt(median(d2) * p)}).  To
restore the v0.0.0.9040 median anchor, pass
\code{sigma = stats::median(as.numeric(stats::dist(scale(X)))^2)}.

Phase 1 speedup (v0.0.0.9042): parallel autotune assumes single-threaded
BLAS for the small kernel operations inside the parallel region. If you
have set a high process-global BLAS thread count (e.g. via
\code{RhpcBLASctl::blas_set_num_threads(8)}), consider resetting to 1 around
\code{krls(..., autotune = TRUE)} calls to avoid oversubscription. The
BLAS-heavy distance computation runs OUTSIDE the parallel region and
benefits from multi-threaded BLAS.

\code{approx = "nystrom"} is currently incompatible with observation
\code{weights} and with user-supplied lambda-search bracket \code{L}/\code{U}/\code{tol}.
Both error with a clear message at fit time. \code{vcov = TRUE} is
supported in the single-fit Nystrom path but the resulting \code{vcov}
is for the m-length dual coefficients, not the n-length kernel
coefficients. Phase 3 may extend these.

Since v0.0.0.9043 \code{krls()} supports an opt-in Nystrom low-rank
approximation via \code{approx = "nystrom"}. Replacing the full n x n
eigendecomposition with an m x m one (m = \code{nystrom_m}, default
\code{ceiling(sqrt(n) * 3)}) gives ~5x speedup at n=2000 and ~10x at
n=5000 with RMSE typically within +10\% of the exact fit on smooth
DGPs. Landmarks default to a uniform random subsample of training
rows; pass \code{landmark_method = "kmeans"} for centroidal landmarks.
Fits are byte-identical at fixed \code{landmark_seed} and \code{seed.cv}.
}
\examples{
set.seed(1)
n <- 100
X <- matrix(rnorm(n * 3), n, 3)
colnames(X) <- c("age", "income", "score")
y <- sin(X[, 1]) + 0.5 * X[, 2]^2 - 0.3 * X[, 3] +
  rnorm(n, sd = 0.2)

fit <- krls(X, y)
fit
fit$avgderivatives           # average marginal effect per variable
summary(fit)

## Formula interface with mixed numeric + factor predictors.
df <- data.frame(age = X[, 1], income = X[, 2],
                 region = factor(sample(c("N", "S"), n, replace = TRUE)),
                 y = y)
fit_f <- krls(y ~ age + income + region, data = df)

## Predictions on new data with pointwise SEs.
Xnew <- matrix(rnorm(20 * 3), 20, 3)
colnames(Xnew) <- colnames(X)
pr <- predict(fit, Xnew, se.fit = TRUE)
head(pr$fit)
head(pr$se.fit)

}
\references{
Hainmueller, J. and C. Hazlett (2014).  "Kernel
Regularized Least Squares: Reducing Misspecification Bias with a
Flexible and Interpretable Machine Learning Approach."  \emph{Political
Analysis} 22(2):143--168.
}
