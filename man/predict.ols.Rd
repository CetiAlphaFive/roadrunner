% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.ols}
\alias{predict.ols}
\title{Predictions from an \code{ols} fit}
\usage{
\method{predict}{ols}(
  object,
  newdata = NULL,
  type = "response",
  se.fit = FALSE,
  interval = c("none", "confidence", "prediction"),
  level = 0.95,
  robust = c("none", "HC0", "HC1", "HC2", "HC3"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"ols"}.}

\item{newdata}{A numeric matrix or data frame of new predictors.
\code{NULL} (default) returns \code{object$fitted.values}.}

\item{type}{Prediction scale. Only \code{"response"} is supported (the
linear predictor coincides with the response for a gaussian fit).}

\item{se.fit}{If \code{TRUE}, the returned vector carries an \code{"se.fit"}
attribute holding the per-row standard error of the fitted mean.
For bagged fits the standard error is the per-row bag standard
deviation. Default \code{FALSE}.}

\item{interval}{Type of interval to return.
\itemize{
\item \code{"none"} (default): point predictions only.
\item \code{"confidence"}: a matrix with columns \code{c("fit", "lwr", "upr")}
for the mean response.
\item \code{"prediction"}: a matrix with columns \code{c("fit", "lwr", "upr")}
for a new observation (adds the residual variance, or the
variance-model variance when the fit was built with
\code{varmod = "const"} or \code{"lm"}).
}}

\item{level}{Confidence/prediction level. Default \code{0.95}.}

\item{robust}{Heteroscedasticity-consistent covariance used for the
standard errors / intervals.
\itemize{
\item \code{"none"} (default): the classical \verb{sigma2 (X'WX)^-1}.
\item \code{"HC0"}, \code{"HC1"}, \code{"HC2"}, \code{"HC3"}: a sandwich estimator.
}}

\item{...}{Currently unused.}
}
\value{
A numeric vector of predictions, or a matrix with columns
\code{c("fit", "lwr", "upr")} when \code{interval != "none"}. With
\code{se.fit = TRUE} the result carries an \code{"se.fit"} attribute.
}
\description{
Returns predictions for new data from a fitted \code{ols} model, with
optional standard errors and exact closed-form confidence or
prediction intervals.
}
\examples{
fit <- ols(mpg ~ wt + hp, data = mtcars)
predict(fit, mtcars[1:5, ], interval = "confidence")
}
