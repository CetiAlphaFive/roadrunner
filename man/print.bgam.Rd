% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.bgam}
\alias{print.bgam}
\title{Print a \code{bgam} fit}
\usage{
\method{print}{bgam}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{x}{An object of class \code{"bgam"}, as returned by \code{\link[=bgam]{bgam()}}.}

\item{digits}{Number of significant digits for numeric output.  Default
\code{max(3L, getOption("digits") - 3L)}.}

\item{...}{Currently ignored.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Prints a compact summary of a fitted component-wise P-spline boosting
model: the call, family, sample size, number of base-learners, boosting
parameters (\code{mstop}, \code{nu}, \code{nknots}, \code{degree}, \code{dpen}, \code{df_target}),
CV-selected \code{mstop_opt} (when \code{autotune = TRUE}), bagging info (when
\code{n.boot > 0}), the top-5 predictors ranked by selection frequency, and
the residual sigma for gaussian fits.
}
\examples{
set.seed(1)
n <- 200
x <- matrix(rnorm(n * 4), n, 4)
y <- sin(x[, 1]) + 0.5 * x[, 2]^2 + rnorm(n, sd = 0.5)
fit <- bgam(x, y, mstop = 50, autotune = FALSE)
print(fit)
}
\seealso{
\code{\link[=bgam]{bgam()}}, \code{\link[=summary.bgam]{summary.bgam()}}, \code{\link[=plot.bgam]{plot.bgam()}}
}
