% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meep.R
\name{meep}
\alias{meep}
\title{Cross-fitted causal ensemble of \code{ares()} and \code{krls()}}
\usage{
meep(
  X,
  y,
  treatment = NULL,
  folds = 5L,
  learners = c("ares", "krls"),
  ensemble = c("stack", "average", "best"),
  arm_models = c("auto", "always", "never"),
  family = NULL,
  treatment_family = NULL,
  tune = c("once", "per_fold", "none"),
  cluster = NULL,
  weights = NULL,
  seed = NULL,
  ares_args = list(),
  krls_args = list(),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{X}{An n-by-p numeric matrix or data.frame of covariates. Factor
columns in a data.frame are expanded by the base fitters.}

\item{y}{Length-n outcome vector.}

\item{treatment}{Optional length-n treatment / exposure vector. \code{NULL}
gives an outcome-only fit (no \code{d_hat_oof}, no arm models).}

\item{folds}{Either a single integer \code{K} (number of folds), or a
length-n integer vector of fold ids that is honored verbatim.}

\item{learners}{Character subset of \code{c("ares", "krls")}, or a named
list of adapter closures (extensibility hook).}

\item{ensemble}{How to combine learners: \code{"stack"} (non-negative least
squares on the OOF matrix), \code{"average"} (equal weights), or \code{"best"}
(pick the single lowest-OOF-loss learner).}

\item{arm_models}{\code{"auto"} fits arm-specific outcome models
(\eqn{\mu_0}, \eqn{\mu_1}) only when the treatment is binary;
\code{"always"} forces them; \code{"never"} suppresses them.}

\item{family}{Outcome family, \code{"gaussian"} or \code{"binomial"}. \code{NULL}
auto-detects.}

\item{treatment_family}{Treatment family, \code{"gaussian"} or \code{"binomial"}.
\code{NULL} auto-detects.}

\item{tune}{One of \code{"once"}, \code{"per_fold"}, \code{"none"}. See \emph{Tuning}.}

\item{cluster}{Optional length-n grouping vector. Folds are assigned at
the cluster level so each cluster lands entirely within one fold.}

\item{weights}{Optional length-n case weights, forwarded to every base
fit and into the NNLS stacking objective.}

\item{seed}{Optional integer seed for fold construction.}

\item{ares_args}{A named list of extra arguments spliced into every
\code{\link[=ares]{ares()}} call.}

\item{krls_args}{A named list of extra arguments spliced into every
\code{\link[=krls]{krls()}} call.}

\item{verbose}{Logical; if \code{TRUE}, print progress per nuisance / fold.}

\item{...}{Currently unused; reserved.}
}
\value{
An object of S3 class \code{"meep"}: a list with \code{y_hat_oof},
\code{d_hat_oof}, \code{mu0_hat_oof}, \code{mu1_hat_oof}, \code{y_resid}, \code{d_resid},
\code{folds}, \code{oof_matrix} (per-nuisance n-by-L matrices), \code{ensemble_weights},
\code{learner_cv_perf}, \code{learners}, \code{family}, \code{treatment_family}, \code{tune},
\code{ensemble}, \code{n}, \code{p}, \code{frozen_hyperparams}, \code{fold_failures},
\code{cluster}, \code{weights}, \code{seed}, and \code{call}.
}
\description{
\code{meep()} produces honest, cross-fitted (out-of-fold) nuisance estimates
for use in Double Machine Learning (DML) and causal-forest workflows.
For an outcome \code{y}, an optional \code{treatment}, and covariates \code{X}, it
cross-fits an ensemble of the package's \code{\link[=ares]{ares()}} (MARS) and \code{\link[=krls]{krls()}}
(Kernel Regularized Least Squares) learners and returns the
out-of-fold predictions \eqn{\hat E[Y\mid X]} and (when \code{treatment} is
supplied) \eqn{\hat E[D\mid X]}, plus the residuals \eqn{Y-\hat Y} and
\eqn{D-\hat D}.
}
\details{
\code{meep()} does \strong{not} estimate a treatment effect. The honest OOF
predictions are meant to be handed to a downstream DML estimator or to
\code{grf::causal_forest()} (see Examples). Because each column of the OOF
prediction matrix already comes from a model that never saw the row it
predicts, the stacking ensemble (\code{ensemble = "stack"}) can fit
non-negative least squares directly on that matrix without a second
layer of nesting -- leakage is already purged column-wise.
}
\section{Family auto-detection}{

When \code{family} is \code{NULL}, a binary \code{y} (exactly two distinct values)
triggers \code{ares(family = "binomial")} and \code{krls(loss = "logistic")};
otherwise the gaussian path is used. The same logic applies to
\code{treatment_family}: a binary treatment is modelled as a propensity
score via logistic regression, a continuous treatment via gaussian
regression. A treatment with three or more distinct values is rejected
(multi-valued / continuous-treatment IRM is out of scope).
}

\section{Tuning (\code{tune})}{

\itemize{
\item \code{"once"} (default) -- autotune each learner on the full data, freeze
the resulting hyperparameters, and refit those frozen settings within
every fold. Standard DML practice: fit honesty (cross-fitting) is what
protects inference; freezing hyperparameters is a deliberate, second-
order compromise.
\item \code{"per_fold"} -- autotune independently inside every fold. Purist
option; slower.
\item \code{"none"} -- the fast path. Call \code{\link[=ares]{ares()}} and \code{\link[=krls]{krls()}} with their own
default arguments (plus anything passed via \code{ares_args} / \code{krls_args}).
No autotune is invoked at all.
}
}

\section{Graceful degradation}{

If a learner errors on a fold (for example, a constant nuisance in a
fold subset), its column for that fold becomes \code{NA}, the ensemble
weights are renormalized over the surviving learners on a per-row basis,
and the event is logged in \verb{$fold_failures}. If \emph{every} learner fails a
fold, \code{meep()} stops.
}

\section{DoubleML recipe}{

To use these nuisances with the \pkg{DoubleML} package, pass
\code{y_hat_oof} / \code{d_hat_oof} as \code{external_predictions} and reuse
\code{fit$folds} as the sample split so the cross-fitting partition matches.
}

\examples{
\donttest{
set.seed(1)
n <- 800; p <- 5
X <- matrix(runif(n * p), n, p)
m  <- 0.4 * X[, 1] + 0.3 * sin(3 * X[, 2])
D  <- m + rnorm(n, sd = 0.3)
g  <- X[, 1]^2 + 0.5 * X[, 3]
Y  <- 1.0 * D + g + rnorm(n, sd = 0.5)        # true ATE = 1.0
fit <- meep(X, Y, treatment = D, folds = 5, seed = 42)
fit
# Hand the honest OOF nuisances to a causal forest:
# grf::causal_forest(X, Y, D, Y.hat = fit$y_hat_oof, W.hat = fit$d_hat_oof)
}

}
\seealso{
\code{\link[=ares]{ares()}}, \code{\link[=krls]{krls()}}
}
