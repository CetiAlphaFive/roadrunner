% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meep.R
\name{predict.meep}
\alias{predict.meep}
\title{Predict from a fitted \code{meep} object}
\usage{
\method{predict}{meep}(
  object,
  newdata,
  nuisance = c("outcome", "treatment", "mu0", "mu1"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{"meep"} object.}

\item{newdata}{A matrix or data.frame of new covariates.}

\item{nuisance}{Which nuisance to predict: \code{"outcome"}, \code{"treatment"},
\code{"mu0"}, or \code{"mu1"}.}

\item{...}{Unused.}
}
\value{
A numeric vector of length \code{nrow(newdata)}.
}
\description{
Out-of-fold predictions exist only for the training rows (and are stored
on the fitted object). For genuinely new data, \code{predict.meep()} refits
each base learner on the \strong{full} training sample -- using the frozen
hyperparameters when \code{tune = "once"} -- then combines the refits with the
stored ensemble weights. Full-data refits are cached lazily in an
environment field on the object and are not serialized.
}
\details{
Predictions on new data can extrapolate; the cross-fitting honesty
guarantee applies only to the training-row OOF predictions.
}
