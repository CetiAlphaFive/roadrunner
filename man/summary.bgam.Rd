% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{summary.bgam}
\alias{summary.bgam}
\alias{print.summary.bgam}
\title{Summarise a \code{bgam} fit}
\usage{
\method{summary}{bgam}(object, ...)

\method{print}{summary.bgam}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{object}{An object of class \code{"bgam"}, as returned by \code{\link[=bgam]{bgam()}}.}

\item{...}{Currently ignored.}

\item{x}{A \code{summary.bgam} object returned by \code{\link[=summary.bgam]{summary.bgam()}}.}

\item{digits}{Number of significant digits for numeric output.  Default
\code{max(3L, getOption("digits") - 3L)}.}
}
\value{
An object of class \code{"summary.bgam"}.  Print it with
\code{\link[=print.summary.bgam]{print.summary.bgam()}}.

Invisibly returns \code{x}.
}
\description{
Returns a \code{summary.bgam} S3 object containing the call, family,
sample and predictor dimensions, boosting parameters (\code{mstop}, \code{nu},
\code{df_target}), CV-selected \code{mstop_opt} (when \code{autotune = TRUE}), bagging
info (when \code{n.boot > 0}), training loss at \code{mstop}, residual sigma for
gaussian fits, and a full predictor selection-frequency table sorted
in descending order.  The selection frequency is the fraction of
boosting iterations in which each predictor was the active base-learner.
}
\examples{
set.seed(1)
n <- 200
x <- matrix(rnorm(n * 4), n, 4)
y <- sin(x[, 1]) + 0.5 * x[, 2]^2 + rnorm(n, sd = 0.5)
fit <- bgam(x, y, mstop = 50, autotune = FALSE)
summary(fit)
}
\seealso{
\code{\link[=bgam]{bgam()}}, \code{\link[=print.bgam]{print.bgam()}}, \code{\link[=plot.bgam]{plot.bgam()}}
}
