% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgam.R
\name{bgam}
\alias{bgam}
\alias{bgam.formula}
\alias{bgam.default}
\title{Component-wise P-spline gradient boosting}
\usage{
bgam(x, ...)

\method{bgam}{formula}(
  x,
  data = NULL,
  subset = NULL,
  na.action = stats::na.omit,
  weights = NULL,
  ...,
  y = NULL
)

\method{bgam}{default}(
  x,
  y,
  family = c("gaussian", "binomial"),
  nu = 0.1,
  mstop = NULL,
  autotune = TRUE,
  mstop_max = 300L,
  nfold = 5L,
  seed.cv = NULL,
  nknots = 20L,
  degree = 3L,
  dpen = 2L,
  df_target = 4,
  lambda_method = c("df", "fixed"),
  lambda_fixed = NULL,
  unpenalized = NULL,
  intercept = TRUE,
  weights = NULL,
  n.boot = 0L,
  seed = NULL,
  nthreads = 1L,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictors (default method), or
a model formula (formula method).}

\item{...}{Passed to the default method from the formula method; otherwise
currently unused.}

\item{data}{A data frame (formula method only).}

\item{subset}{Optional integer or logical row-subsetting vector passed to
\code{\link[stats:model.frame]{stats::model.frame()}} (formula method only).}

\item{na.action}{NA-handling function passed to \code{\link[stats:model.frame]{stats::model.frame()}}.
Default \code{\link[stats:na.omit]{stats::na.omit()}}.  The default method drops rows with any \code{NA}
in \code{x} and emits a warning; rows with \code{NA} in \code{y} are always rejected.}

\item{weights}{Optional strictly positive numeric weight vector of length
\code{nrow(x)}.  For \code{"gaussian"}, weights scale the pseudo-residual sum of
squares used for base-learner selection and the Cholesky prefactor.  For
\code{"binomial"}, weights scale the IRLS working weights
\eqn{w_i^{(\text{eff})} = w_i \mu_i (1 - \mu_i)}{w_i * mu_i * (1 - mu_i)}.
Default \code{NULL} (unweighted).}

\item{y}{Response vector of length \code{nrow(x)}.  For \code{family = "binomial"},
must be binary: 0/1 numeric, \code{logical}, or a two-level \code{factor} (second
level = 1).}

\item{family}{Response family: \code{"gaussian"} (default, squared-error loss)
or \code{"binomial"} (logistic / binomial deviance, binary response).}

\item{nu}{Boosting shrinkage / learning rate.  Must be in \verb{(0, 1]}.
Smaller values require more iterations but produce smoother solution
paths.  Default \code{0.1}.}

\item{mstop}{Number of boosting iterations to run.  If \code{NULL} and
\code{autotune = FALSE}, defaults to \code{mstop_max}.  If \code{autotune = TRUE},
\code{mstop} is set to the CV-selected \code{mstop_opt}.}

\item{autotune}{If \code{TRUE} (default), selects \code{mstop} by k-fold
cross-validation over the grid \code{seq(10, mstop_max, by = 10)} using MSE
(gaussian) or binomial deviance (binomial) as the criterion.  A
successive-halving early-stop rule terminates the grid scan once CV loss
rises more than 2\% of its range above the running best.}

\item{mstop_max}{Maximum iterations for the CV grid (and the default when
\code{autotune = FALSE} and \code{mstop = NULL}).  Reduced automatically to
\code{max(50, 2 * n)} when \code{n < 100}.  Default \code{300L}.}

\item{nfold}{Number of CV folds.  Default \code{5L}.}

\item{seed.cv}{Integer seed for CV fold construction.  Default \code{NULL}.
Binomial CV folds are stratified by response class.}

\item{nknots}{Number of interior B-spline knots per predictor.  Knots are
placed at equally-spaced quantiles of the predictor column.  Default
\code{20L}, giving \code{K_j = nknots + degree + 1 = 24} columns per base-learner
at defaults.}

\item{degree}{B-spline polynomial degree (1 to 5).  Default \code{3L} (cubic
B-splines).}

\item{dpen}{Order of the difference penalty (1 to \code{degree}).  Default \code{2L}
(second-order penalty, analogous to a thin-plate-spline roughness penalty;
Eilers & Marx 1996).}

\item{df_target}{Target effective degrees of freedom per base-learner,
i.e. \code{tr(S_j(lambda_j))}.  Must be \verb{> 1}.  Default \code{4}.  The ridge
penalty \code{lambda_j} is calibrated per predictor via \code{stats::uniroot}.}

\item{lambda_method}{\code{"df"} (default): calibrate \code{lambda_j} from
\code{df_target} per predictor.  \code{"fixed"}: supply \code{lambda_j} directly via
\code{lambda_fixed}.}

\item{lambda_fixed}{Numeric scalar or length-p vector of lambda values.
Required when \code{lambda_method = "fixed"}; ignored otherwise.}

\item{unpenalized}{Optional character vector of predictor column names to
enter as linear (unpenalised, no-spline) base-learners.  Useful for
known-linear covariates or binary dummies.}

\item{intercept}{If \code{TRUE} (default), initialise the additive predictor at
\code{mean(y)} (gaussian) or \code{logit(clamp(mean(y), 0.001, 0.999))} (binomial).
If \code{FALSE}, initialise at 0.}

\item{n.boot}{Number of bootstrap replicates for bagging.  Default \code{0L}
(no bagging).  When \verb{> 0}, \code{predict()} returns the bag mean across the
central fit and surviving replicates.  Hyperparameters are frozen at
the central-fit values; no re-autotune per replicate.}

\item{seed}{Optional integer RNG seed for the bagging loop.  Default
\code{NULL}.}

\item{nthreads}{TBB thread count for the parallel predictor scan per
boosting iteration.  \code{0} = use all available TBB threads.  \code{1L}
(default) = serial execution.  Results are byte-identical across all
\code{nthreads} values.}
}
\value{
An S3 object of class \code{"bgam"}, a named list with the following
key fields (full contract: 25+ fields):
\describe{
\item{\code{coefficients}}{List of p accumulated (nu-scaled) beta vectors,
one per predictor.  Zero-vector if the predictor was never selected.}
\item{\code{fitted.values}}{Fitted values on the response scale
(probabilities for binomial; identical to \code{linear.predictors} for
gaussian).  For bagged fits, this is the bag mean.}
\item{\code{linear.predictors}}{Fitted values on the link scale.}
\item{\code{residuals}}{Residuals on the response scale (gaussian:
\code{y - fitted}; binomial: deviance residuals).}
\item{\code{selection_path}}{Integer vector of length \code{mstop}, 1-based
predictor index selected at each iteration.}
\item{\code{selection_frequency}}{Named numeric vector of length p; fraction
of iterations each predictor was selected.  Sums to 1.}
\item{\code{loss_path}}{Numeric vector of length \code{mstop}; training loss
(MSE or deviance) after each iteration.}
\item{\code{mstop}}{Integer; iterations run (equals \code{mstop_opt} when
\code{autotune = TRUE}).}
\item{\code{cv}}{List with \verb{$grid}, \verb{$cv_loss}, \verb{$mstop_opt} when
\code{autotune = TRUE}; \code{NULL} otherwise.}
\item{\code{boot}}{List with \verb{$n.boot} and \verb{$fits} when \code{n.boot > 0};
\code{NULL} otherwise.}
\item{\code{base_learners}}{List of p per-predictor lists, each containing
\verb{$knots}, \verb{$K}, \verb{$degree}, \verb{$dpen}, \verb{$lambda}, \verb{$range}, \verb{$chol},
\verb{$B_train}.}
}
See also: \code{predict.bgam}, \code{print.bgam}, \code{summary.bgam}, \code{plot.bgam}.
}
\description{
Fits a smooth, strictly additive model via component-wise functional
gradient boosting with penalised B-spline (P-spline) base-learners, one
per predictor column. At each of \code{mstop} boosting iterations the algorithm
selects the single base-learner that most reduces the current loss (squared
error for gaussian, binomial deviance for binomial), updates only that
component by a shrinkage-scaled Cholesky-backed ridge solve, and repeats.
The result is a smooth GAM with implicit variable selection: predictors
that are never selected retain a zero coefficient vector, and
\verb{$selection_frequency} (fraction of iterations each predictor was chosen)
serves as a natural importance metric.
}
\details{
Each base-learner is a cubic (or general degree-\code{d}) B-spline with
\code{nknots} interior knots placed at equally-spaced quantiles of the
predictor, penalised by a \code{dpen}-th order difference-penalty matrix
\eqn{D^T D}{D'D} scaled by a ridge parameter \eqn{\lambda_j}{lambda_j}
calibrated so the effective degrees of freedom of the base-learner equals
\code{df_target}.  Near-constant predictors fall back automatically to
unpenalised linear base-learners (with a warning).

\code{bgam()} complements the rest of the roadrunner lineup: it is more
interpretable than \code{krls()} (explicit additive components, no kernel
matrix), handles non-linearity without manual basis specification unlike
\code{ols()}, and targets smooth monotonic / additive signals where \code{ares()}'s
piecewise-linear hinge functions may be less efficient.  The complexity per
fit is O(n * p * K) per iteration (K ~ \code{nknots + degree + 1}), making it
practical for large-n, moderately large-p settings where \code{krls()} becomes
memory-bound.

The formula method builds a standard model frame (factors expanded via
\code{model.matrix}, formula's \code{-1} disables the intercept initialisation),
stores \verb{$terms} and \verb{$xlevels} for \code{predict()}, and dispatches to the
default method.  The default method takes a numeric matrix directly.
}
\examples{
set.seed(1)
n <- 200
x <- matrix(rnorm(n * 4), n, 4)
y <- sin(x[, 1]) + 0.5 * x[, 2]^2 + rnorm(n, sd = 0.5)

# Fit with fixed mstop (fast, no CV)
fit <- bgam(x, y, mstop = 100, autotune = FALSE, nthreads = 1)
print(fit)
predict(fit, x[1:3, ])

# Formula interface
df <- data.frame(y = y, x)
fit2 <- bgam(y ~ ., data = df, mstop = 50, autotune = FALSE)
head(fit2$selection_frequency)
}
\references{
Buehlmann, P. and Yu, B. (2003). Boosting with the L2 loss: Regression and
classification. \emph{Journal of the American Statistical Association},
98(462):324--339. \doi{10.1198/016214503000125}

Eilers, P. H. C. and Marx, B. D. (1996). Flexible smoothing with B-splines
and penalties. \emph{Statistical Science}, 11(2):89--121.
\doi{10.1214/ss/1038425655}

Hofner, B., Mayr, A., Robinzonov, N. and Schmid, M. (2014). Model-based
boosting in R: A hands-on tutorial using the R package mboost.
\emph{Computational Statistics}, 29(1--2):3--35.
\doi{10.1007/s00180-012-0382-5}

Schmid, M. and Hothorn, T. (2008). Boosting additive models using
component-wise P-splines. \emph{Computational Statistics and Data Analysis},
53(2):298--311. \doi{10.1016/j.csda.2008.09.009}
}
