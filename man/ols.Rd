% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ols.R
\name{ols}
\alias{ols}
\alias{ols.formula}
\alias{ols.default}
\title{Ordinary and weighted least squares}
\usage{
ols(x, ...)

\method{ols}{formula}(
  x,
  data = NULL,
  subset = NULL,
  na.action = stats::na.omit,
  weights = NULL,
  ...,
  y = NULL
)

\method{ols}{default}(
  x,
  y,
  weights = NULL,
  intercept = TRUE,
  n.boot = 0L,
  seed = NULL,
  varmod = c("none", "const", "lm"),
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictors, or a model
formula when calling the formula method.}

\item{...}{Currently ignored.}

\item{data}{A data frame. Used only by the formula method.}

\item{subset}{Optional row-subsetting vector for the formula method
(integer indices or logical), passed through to
\code{\link[stats:model.frame]{stats::model.frame()}}. Ignored by the default method.}

\item{na.action}{Strategy for missing values, passed to
\code{\link[stats:model.frame]{stats::model.frame()}} for the formula method. Default
\code{\link[stats:na.omit]{stats::na.omit()}}. For the default method, rows with any \code{NA} in
\code{x} or \code{y} are dropped with a warning.}

\item{weights}{Optional non-negative numeric vector of length
\code{nrow(x)} for weighted least squares. Default \code{NULL} (unweighted).
Negative, \code{NA}, \code{NaN}, or \code{Inf} weights are rejected; zero weights
are rejected (drop the row instead).}

\item{y}{A numeric response vector with length \code{nrow(x)}.}

\item{intercept}{Logical; include an intercept term. Default \code{TRUE}.
Honoured by the default method; the formula method follows the
formula's own intercept specification.}

\item{n.boot}{Number of bootstrap replicate fits for bagging.
Default \code{0} (no bagging). When \verb{> 0}, \code{predict()} averages over the
replicates plus the central fit and (with \code{se.fit = TRUE}) returns a
per-prediction bag standard deviation.}

\item{seed}{Optional integer seed for the bagging bootstrap draws.
Pass an integer for reproducible bagging; \code{NULL} (default) uses the
current RNG state. The user's RNG stream is restored on exit when a
seed is supplied.}

\item{varmod}{Residual variance model used by \code{predict()} for
prediction intervals.
\itemize{
\item \code{"none"} (default): the closed-form prediction interval uses the
fitted residual standard error directly.
\item \code{"const"}: stores a single residual SD.
\item \code{"lm"}: fits a small linear model of \verb{|resid|} on the fitted
values to allow simple yhat-dependent heteroscedasticity. Shares
the \code{ares()} variance-model helper.
}}
}
\value{
An object of class \code{"ols"}: a list with the fitted
\code{coefficients}, \code{fitted.values}, \code{residuals}, \code{sigma2} and \code{sigma},
\code{df.residual}, the classical variance-covariance matrix \code{vcov}, the
numerical \code{rank}, \code{hatvalues}, \verb{(X'WX)^-1} as \code{XtXinv}, the design
matrix \code{X} and response \code{y}, echoed \code{weights}, the \code{call}, and
(when requested) \verb{$varmod} and \verb{$boot} fields.
}
\description{
Fits a linear regression model by ordinary least squares, or by
weighted least squares when \code{weights} are supplied. The solver is a
weighted economy-QR factorisation in C++ (Armadillo/LAPACK); a
rank-deficient design matrix is rejected with a clear error rather
than silently dropping columns.
}
\details{
Two interfaces are provided. The formula method takes a model formula
and a data frame. The default method takes a numeric predictor matrix
\code{x} and a numeric response vector \code{y}.

Optional bagging (\code{n.boot > 0}) refits the model on bootstrap-weight
resamples; \code{predict()} then returns the bag mean. Bagging is a serial
loop and is byte-identical across thread counts.
}
\examples{
fit <- ols(mpg ~ wt + hp, data = mtcars)
print(fit)
predict(fit, mtcars[1:3, ])
}
\references{
MacKinnon, J. G. and White, H. (1985). Some heteroskedasticity-consistent
covariance matrix estimators with improved finite sample properties.
\emph{Journal of Econometrics} 29(3):305-325.
}
