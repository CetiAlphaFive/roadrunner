% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.logreg}
\alias{predict.logreg}
\title{Predictions from a \code{logreg} fit}
\usage{
\method{predict}{logreg}(
  object,
  newdata = NULL,
  type = c("link", "response", "class"),
  se.fit = FALSE,
  threshold = 0.5,
  robust = c("none", "HC0", "HC1", "HC2", "HC3"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"logreg"}.}

\item{newdata}{A numeric matrix or data frame of new predictors.
\code{NULL} (default) returns predictions for the training data.}

\item{type}{Prediction scale.
\itemize{
\item \code{"link"}: the linear predictor (log-odds).
\item \code{"response"} (default): the fitted probability.
\item \code{"class"}: a hard 0/1 (or factor) label obtained by thresholding
the fitted probability at \code{threshold}.
}}

\item{se.fit}{If \code{TRUE}, the returned object carries an \code{"se.fit"}
attribute holding the per-row standard error. For \code{type = "link"}
the SE is on the log-odds scale; for \code{type = "response"} it is the
delta-method SE of the probability. \code{se.fit} is not available for
\code{type = "class"}. For bagged fits the SE is the per-row bag
standard deviation of the predicted probability. Default \code{FALSE}.}

\item{threshold}{Probability cut-off for \code{type = "class"}. Default
\code{0.5}.}

\item{robust}{Heteroscedasticity-consistent covariance used for the
standard errors.
\itemize{
\item \code{"none"} (default): the classical maximum-likelihood covariance.
\item \code{"HC0"}, \code{"HC1"}, \code{"HC2"}, \code{"HC3"}: a sandwich estimator.
}}

\item{...}{Currently unused.}
}
\value{
A numeric vector of predictions on the requested scale (a
factor when \code{type = "class"} and the fit was built from a factor
response). With \code{se.fit = TRUE} the result carries an \code{"se.fit"}
attribute.
}
\description{
Returns predictions for new data from a fitted \code{logreg} model on the
link, probability, or class scale, with optional standard errors.
}
\examples{
df <- data.frame(y = as.integer(mtcars$am), mtcars[c("wt", "hp")])
fit <- logreg(y ~ wt + hp, data = df)
predict(fit, df[1:5, ], type = "response")
predict(fit, df[1:5, ], type = "class")
}
