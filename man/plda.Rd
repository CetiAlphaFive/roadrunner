% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plda.R
\name{plda}
\alias{plda}
\alias{plda.default}
\alias{plda.formula}
\title{Penalized linear discriminant analysis}
\usage{
plda(x, ...)

\method{plda}{default}(
  x,
  y,
  K = NULL,
  lambda = NULL,
  penalty = c("L1", "fused"),
  lambda2 = NULL,
  autotune = TRUE,
  nfold = 5L,
  lambda_grid = NULL,
  maxit = 100L,
  tol = 0.000001,
  nthreads = 0L,
  ...
)

\method{plda}{formula}(formula, data = NULL, ...)
}
\arguments{
\item{x}{Numeric predictor matrix, or a formula for the formula interface.}

\item{...}{Passed to methods.}

\item{y}{Factor (or coercible) class label of length \code{nrow(x)}.}

\item{K}{Number of discriminant vectors (\verb{<= G-1}). If supplied, \code{K} is
fixed; if \code{NULL} (default), \code{K} is chosen by cross-validation along with
\code{lambda}. When \code{autotune = FALSE}, \code{NULL} means \code{G-1}.}

\item{lambda}{Magnitude penalty. Required when \code{autotune = FALSE}.}

\item{penalty}{\code{"L1"} (default) or \code{"fused"}.}

\item{lambda2}{Fused-lasso difference penalty (used when \code{penalty = "fused"}).}

\item{autotune}{If \code{TRUE} (default), cross-validate \code{lambda} (and \code{K}).}

\item{nfold}{CV folds.}

\item{lambda_grid}{Optional CV grid.}

\item{maxit}{MM solver maximum iterations.}

\item{tol}{MM solver convergence tolerance.}

\item{nthreads}{Integer. Number of worker threads for the cross-validation
autotune (\code{autotune = TRUE}). \code{0} (the default, taken from
\code{getOption("roadrunner.nthreads")} when set) uses
\code{RcppParallel::defaultNumThreads()}. The CV fold loop is parallelised with
fixed per-fold output slots and a serial-order reduction, so the fitted
discriminants are byte-identical regardless of \code{nthreads}. Ignored when
\code{autotune = FALSE} (a single fit is already sequential).}

\item{formula}{A model formula; response on the left, predictors on the right.}

\item{data}{A data frame (or environment) containing the formula variables.}
}
\value{
An object of S3 class \code{"plda"}.
}
\description{
Penalized Fisher's linear discriminant (Witten & Tibshirani 2011) with L1 or
fused-lasso penalties, multi-class support, and built-in cross-validation.
}
