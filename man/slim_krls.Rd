% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krls.R
\name{slim_krls}
\alias{slim_krls}
\title{Strip heavy fields off a KRLS fit}
\usage{
slim_krls(fit, keep_predict = TRUE)
}
\arguments{
\item{fit}{A \code{krls_rr} fit.}

\item{keep_predict}{Logical. If \code{TRUE} (default), keep enough
state for \code{predict()} to run.}
}
\value{
A \code{krls_rr} object with heavy fields removed and
\verb{$slimmed = TRUE}.
}
\description{
Removes large \verb{n x n} (or \verb{n x m}) intermediates from a fitted
\code{krls_rr} object so it serialises (\code{saveRDS}) much smaller.
Two modes:
}
\details{
\itemize{
\item \code{keep_predict = TRUE} (default): keeps everything
\code{predict.krls_rr()} needs (coeffs, sigma / sigma_vec, lambda,
the standardisation stats, factor bookkeeping, and Nystrom
landmarks where applicable). Drops the \code{K}, eigenvector
matrices \code{V} / \code{Vsq}, projected response \code{Vty}, and
\code{vcov.c}.
\item \code{keep_predict = FALSE}: keeps only inspection-grade summary
fields (\code{coeffs}, \code{R2}, \code{Looe}, \code{avgderivatives},
\code{var.avgderivatives}, \code{sigma}, \code{sigma_vec}, \code{lambda},
\code{Neffective}). \code{predict()} will refuse to run.
}

The returned object carries the flag \verb{$slimmed = TRUE}.
\code{unslim_krls()} is a no-op (heavy intermediates can only be
rebuilt by refitting).
}
\keyword{internal}
