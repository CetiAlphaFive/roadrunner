% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.bgam}
\alias{predict.bgam}
\title{Predictions from a \code{bgam} fit}
\usage{
\method{predict}{bgam}(
  object,
  newdata = NULL,
  type = c("response", "link", "terms"),
  se.fit = FALSE,
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{An object of class \code{"bgam"}, as returned by \code{\link[=bgam]{bgam()}}.}

\item{newdata}{A numeric matrix or data frame of new predictor values with
the same columns (in any order) as the training data.  \code{NULL} (default)
returns stored training predictions: \verb{$fitted.values} for non-bagged
fits, bag mean for bagged fits.  Out-of-vocabulary factor levels in
\code{newdata} cause an informative error.}

\item{type}{Prediction scale:
\describe{
\item{\code{"response"}}{(default) Fitted probabilities for
\code{family = "binomial"}; fitted values (identical to link scale) for
\code{family = "gaussian"}.}
\item{\code{"link"}}{The linear predictor (additive predictor on the link
scale).  Identical to \code{"response"} for gaussian.}
\item{\code{"terms"}}{An \code{nrow(newdata)} x \code{p} numeric matrix of per-
predictor component contributions.  Column \code{j} holds
\code{B_new_j \%*\% beta_j}.  Column names match \code{object$predictor_names}.
Row sums of all columns plus \code{object$intercept_value} equal the
link-scale prediction.}
}}

\item{se.fit}{If \code{TRUE}, returns a list with elements \verb{$fit} (predictions
as described above) and \verb{$se.fit} (plug-in standard errors summed
across active base-learners).  For predictor j active at observation i,
the component variance is \code{diag(B_j[i,,drop=FALSE] \%*\% solve(A_j) \%*\% t(B_j[i,,drop=FALSE])) * sigma2} where \verb{A_j = B_j'B_j + lambda_j * D_j'D_j}.  For binomial, delta-method SE on the response scale is
applied: \code{se_mu = se_eta * mu_hat * (1 - mu_hat)}.
Default \code{FALSE}.}

\item{level}{Unused; reserved for a future prediction-interval interface.
Default \code{0.95}.}

\item{...}{Currently unused.}
}
\value{
\describe{
\item{\code{se.fit = FALSE}}{A numeric vector of length \code{nrow(newdata)}, or
a numeric matrix of dimension \code{nrow(newdata)} x \code{p} when
\code{type = "terms"}.}
\item{\code{se.fit = TRUE}}{A list with elements \verb{$fit} (as above) and
\verb{$se.fit} (numeric vector of per-observation standard errors).}
}
For bagged fits the bag mean is returned (se.fit is not available for
bagged fits).
}
\description{
Returns predictions from a fitted component-wise P-spline boosting model.
New B-spline bases are constructed for \code{newdata} using the stored knot
sequences; values outside the training range are extrapolated by the
natural boundary behaviour of the de Boor recursion (polynomial
extrapolation).  For bagged fits (\code{n.boot > 0}), the bag mean across all
surviving replicates is returned.
}
\examples{
set.seed(1)
n <- 200
x <- matrix(rnorm(n * 3), n, 3)
y <- sin(x[, 1]) + 0.5 * x[, 2] + rnorm(n, sd = 0.5)
fit <- bgam(x, y, mstop = 50, autotune = FALSE)
predict(fit, x[1:5, ])

# Per-component contributions
terms_mat <- predict(fit, x, type = "terms")
head(terms_mat)

# Plug-in standard errors
p_se <- predict(fit, x[1:5, ], se.fit = TRUE)
p_se$se.fit
}
