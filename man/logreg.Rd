% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logreg.R
\name{logreg}
\alias{logreg}
\alias{logreg.formula}
\alias{logreg.default}
\title{Binary logistic regression}
\usage{
logreg(x, ...)

\method{logreg}{formula}(
  x,
  data = NULL,
  subset = NULL,
  na.action = stats::na.omit,
  weights = NULL,
  ...,
  y = NULL
)

\method{logreg}{default}(
  x,
  y,
  weights = NULL,
  intercept = TRUE,
  n.boot = 0L,
  seed = NULL,
  maxit = 25L,
  tol = 1e-08,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictors, or a model
formula when calling the formula method.}

\item{...}{Currently ignored.}

\item{data}{A data frame. Used only by the formula method.}

\item{subset}{Optional row-subsetting vector for the formula method
(integer indices or logical), passed through to
\code{\link[stats:model.frame]{stats::model.frame()}}. Ignored by the default method.}

\item{na.action}{Strategy for missing values, passed to
\code{\link[stats:model.frame]{stats::model.frame()}} for the formula method. Default
\code{\link[stats:na.omit]{stats::na.omit()}}. For the default method, rows with any \code{NA} in
\code{x} or \code{y} are dropped with a warning.}

\item{weights}{Optional non-negative numeric vector of length
\code{nrow(x)} of prior weights for weighted logistic regression. Default
\code{NULL} (unweighted). Negative, \code{NA}, \code{NaN}, or \code{Inf} weights are
rejected; zero weights are rejected (drop the row instead).}

\item{y}{A binary response of length \code{nrow(x)}: a 0/1 numeric vector, a
logical vector, or a factor with exactly two levels (the second
level is treated as the "success" class).}

\item{intercept}{Logical; include an intercept term. Default \code{TRUE}.
Honoured by the default method; the formula method follows the
formula's own intercept specification.}

\item{n.boot}{Number of bootstrap replicate fits for bagging.
Default \code{0} (no bagging). When \verb{> 0}, \code{predict()} averages the
fitted probabilities over the replicates plus the central fit.}

\item{seed}{Optional integer seed for the bagging bootstrap draws.
Pass an integer for reproducible bagging; \code{NULL} (default) uses the
current RNG state. The user's RNG stream is restored on exit when a
seed is supplied.}

\item{maxit}{Maximum number of IRLS iterations. Default \code{25}.}

\item{tol}{Convergence tolerance on the relative change in deviance
between IRLS iterations. Default \code{1e-8}.}
}
\value{
An object of class \code{"logreg"}: a list with the fitted
\code{coefficients}, \code{fitted.values} (probabilities), \code{linear.predictors},
deviance and working \code{residuals}, \code{deviance}, \code{null.deviance}, \code{aic},
\code{df.residual}, \code{df.null}, the classical variance-covariance matrix
\code{vcov}, the IRLS \code{iter} count and \code{converged} flag, the numerical
\code{rank}, \code{hatvalues}, \verb{(X'WX)^-1} as \code{XtWXinv}, the design matrix \code{X}
and the (numeric 0/1) response \code{y}, echoed \code{weights}, the \code{call},
and (when requested) a \verb{$boot} field.
}
\description{
Fits a binary logistic regression model by iteratively reweighted least
squares (Fisher scoring). The IRLS step is a weighted economy-QR solve
in C++ (Armadillo/LAPACK), mirroring the \code{\link[=ols]{ols()}} engine. The response
must be binary; it may be supplied as a 0/1 numeric vector, a logical
vector, or a two-level factor.
}
\details{
Two interfaces are provided. The formula method takes a model formula
and a data frame. The default method takes a numeric predictor matrix
\code{x} and a binary response \code{y}.

Optional bagging (\code{n.boot > 0}) refits the model on bootstrap-weight
resamples; \code{predict()} then returns the bag-mean probability. Bagging
is a serial loop and is byte-identical across thread counts.

When the IRLS iteration fails to converge within \code{maxit} steps -- the
usual signature of (quasi-)complete separation -- the fit is returned
with \code{converged = FALSE} and a warning is emitted.
}
\examples{
df <- data.frame(y = as.integer(mtcars$am), mtcars[c("wt", "hp")])
fit <- logreg(y ~ wt + hp, data = df)
print(fit)
predict(fit, df[1:3, ], type = "response")
}
\references{
McCullagh, P. and Nelder, J. A. (1989). \emph{Generalized Linear Models},
2nd ed. Chapman and Hall.

MacKinnon, J. G. and White, H. (1985). Some heteroskedasticity-consistent
covariance matrix estimators with improved finite sample properties.
\emph{Journal of Econometrics} 29(3):305-325.
}
